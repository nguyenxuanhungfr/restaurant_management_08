<% provide :title, t("admin.dish.new_dish") %>
<% provide :button_text, t("admin.dish.save_dish") %>
<div class="row">
  <div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
      <div class="card-body">
        <h1><%= t "admin.dish.new_dish" %></h1>
          <%= render "form" %>
      </div>
    </div>
  </div>
</div>
