<% provide :title, t("admin.booking.edit_booking") %>
<% provide :button_text, t("admin.booking.save_booking") %>
<div class="row">
  <div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
      <div class="card-body">
        <h1><%= t "admin.booking.edit_booking" %></h1>
        <%= render "form" %>
      </div>
    </div>
  </div>
</div>
